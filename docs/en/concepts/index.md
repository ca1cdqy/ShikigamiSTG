# Concepts

This section explains the framework model instead of listing API members.

Planned topics:

- Worlds, entities, components, and systems
- Responsibilities of `GameDefinition`, `Runtime`, and `Session`
- Fixed ticks, phase commits, and determinism
- `StageProgram` and immediate low-level APIs
- Actor convenience types and low-level component APIs
- Presentation events, snapshots, and asset realization
