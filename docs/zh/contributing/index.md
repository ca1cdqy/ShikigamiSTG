# 文档贡献

人工文档以中文为主源。修改流程如下：

1. 先修改 `docs/zh` 中的中文页面。
2. 保持标题层级、代码示例和链接稳定。
3. 将内容翻译到 `docs/en` 的相同相对路径。
4. 不在教程中复制 API 签名，改为链接 Doxygen 页面。
5. 代码示例应进入可编译测试或示例项目，文档只引用经过验证的版本。

Doxygen 注释必须直接使用英文，因为它们对应唯一的 C++ API 契约，不维护双份源码注释。
